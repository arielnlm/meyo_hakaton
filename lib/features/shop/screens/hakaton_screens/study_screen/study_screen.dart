import 'package:flutter/material.dart';

class StudyScreenH extends StatelessWidget {
  const StudyScreenH({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}